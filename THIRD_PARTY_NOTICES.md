# Third-party notices

The runtime includes JSZip for reading XLSX ZIP containers and making a review-data backup ZIP. JSZip is used under its MIT license; the complete included license is `web/JSZIP-LICENSE.txt`. Browser and Node runtime APIs supply the remaining pilot functionality. No font files are included.

User-supplied VMS source is retained as reference material, not relicensed as third-party code. Its dependency declarations do not imply those dependencies were installed or that the recovered application was executed. The local pilot has no npm package dependencies.
