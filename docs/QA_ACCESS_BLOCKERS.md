# QA access and dependency blockers

## Published and activated — 13 September 2026

The user requested publication after the bug fixes. Runtime commit **f5d55146cd7428cc72d6f6ef6bcc52ead48b63df** is deployed at https://purchase.dvjassociates.com. Coolify deployment **fghgsunna2djj2wpin1ytwao** finished successfully; application status is running:healthy and /api/health returns HTTP 200. Eleven served assets match the release commit byte for byte.

The independent Manager preset was saved through the live Admin browser, with the confirmed reason and self-approval acknowledgement. All 13 stages include MANAGER and survive reload. Ashok and Suresh were both verified as active MANAGER accounts assigned to LAE Import; their existing roles/scopes were not changed. Approval-control revision is **2**. Other configured role grants remain intact, and Admin retains later per-stage editing.

The deployment preserved the business state exactly: 29 orders, including all 15 retained QA POs. The policy save appended exactly one APPROVAL_CONTROLS_UPDATED event; orders, payments, files, users, vendors, items, bases and costs stayed unchanged. The existing /app/data persistent volume and domain configuration are unchanged. No new test purchase, deletion, payment or shipment action was performed.

Live verification: 10 release-browser checks, 27 activation checks, 11 asset comparisons and health checks passed. Evidence remains in ignored test-output/manager-release and sanitized Coolify deployment reports. Earlier local verification remains 135 native checks, 33 setup checks, 29 approval-control checks, 38 import-lifecycle checks and six complete flows across two isolated Manager accounts.

This establishes deployment, published entry points, retained data and effective Manager approval policy. It does not replace the original live 340-scenario QA ledger or claim a new end-to-end login run as Ashok/Suresh. Real supplier/quote/evidence prerequisites and the previously documented infrastructure and finance/date-policy acceptance items remain. Earlier “local only / activation pending” paragraphs below describe pre-release history.


## Current local resolution — DEC-030 / WF-024, 13 September 2026

**Operational fixes implemented and tested locally; not deployed or activated on live.** The user confirmed independent access for every Purchase Manager. The build includes the 13 prior QA fixes, an Admin-saved preset for all 13 approval stages, Manager access visibility, item upload/mapping, vendor/price imports, vendor audit/source history and quote-backed freight setup recovery.

| Verification | Result |
|---|---|
| Native regression | 135 PASS, 0 FAIL |
| Manager setup, server and standalone review | 33 PASS; two separate Manager accounts; new supplier → base → ERP → technical/brand/artwork approvals; imports/history; missing benchmark recovery |
| Approval controls and restoration | 29 PASS |
| Import lifecycle | 38 PASS |
| First isolated Manager account | 3 complete flows, all PORT_ARRIVED and SETTLED with zero balance |
| Second isolated Manager account | 3 complete flows, all PORT_ARRIVED and SETTLED with zero balance |
| Build / diff | Standalone regenerated; whitespace check passed; mobile booking screenshot reviewed |

The six flows cover USD advance/BL balance, CNY 10/20/70 with multiple attachments, and USD credit with two partial shipments. Audit checks require every workflow action to belong to the single Manager for that flow. These are synthetic local accounts, not current login tests of the real Ashok/Suresh accounts.

Evidence under ignored test-output: manager-independence-native.txt; manager-setup/2026-09-13T01-12-00-775Z/report.json; approval-controls/2026-09-13T01-15-21-183Z/report.json; import-preview/2026-09-13T01-15-31-380Z/report.json; manager-relaxed-workflows/2026-09-13T01-07-37-160Z/report.json and 2026-09-13T01-13-06-944Z/report.json.

Initial setup test failures were harness assertions: a subtitle hidden in Minimal and duplicate header/card booking locators. They now assert visible access rows and target the shipment-card action. An initial zero-price expectation was corrected to the documented master policy; no formula was changed to satisfy a test.

| Original blocker | Resolution / remaining prerequisite |
|---|---|
| ACCESS-001 | Preset covers technical rejection and every other approval stage; Admin must save it on live. |
| ACCESS-002 / ACCESS-003 | User expressly retained Admin-only account/policy/delete/restore controls. They are not purchase-progress steps. |
| DEP-001 | ERP creation and imported-item mapping tested through technical/brand/artwork approvals; publish and live-retest. |
| DEP-002 | Item upload plus vendor/price templates, validation, atomic commit and source history implemented; live-retest required. |
| DEP-003 | Vendor View shows local actor and before/after audit; no missing historical audit fabricated. |
| DEP-004 | Supplied quote recovery implemented; QA12 still needs a correct actual 20GP quote. No operational rate invented. |
| ENV-001 / ENV-002 | Separate local Manager accounts and authorization checks pass. Wider fault/restore/scale/live-auth preconditions are not waived. |

**Live completion still requires deployment and one-time Admin activation.** Verify Ashok/Suresh are active MANAGER accounts assigned to LAE Import, then save the preset and retest with retained live records. The prior live runner did not respond and the browser connector listed no sessions. A separate visible Playwright window is open at the live sign-in page for Admin login. No credentials were read, no live grants changed and no live QA creations deleted or modified.

See [MANAGER_WORKFLOW.md](MANAGER_WORKFLOW.md) for activation and the complete operating sequence. The original live ledger remains historical 235 PASS / 21 FAIL / 84 BLOCKED. Local regressions do not relabel those live cases as passed. Real evidence/quotes, infrastructure acceptance and unresolved finance/date policies still apply. No unconditional soft-launch sign-off is claimed.


Continuation: 13 September 2026 IST. User direction: enumerate restricted steps, keep testing independent scenarios, retain all live creations. Ashok remains signed in as Purchase Manager. This register distinguishes permission restrictions from missing UI and environment requirements; none is counted as a passed business workflow.

| ID | Step / affected scenarios | Observed blockage | Who or what can unblock it | Retained record / evidence |
|---|---|---|---|---|
| ACCESS-001 | Technical revision rejection, QA-067 | QAAS01 QA-1.1 is PENDING. Approve appears; Reject does not appear for Ashok. | An Admin can reject it or review the configured Technical rejection grant and delegate deliberately. A configured Product Manager may also qualify; current grants are not visible to Ashok. | QAAS01 / QA-1.1; r2plm-009. Keep pending for review. |
| ACCESS-002 | User creation, role editing, division assignment and approval-control editing | Users & settings exposes users and policy information but no admin mutation controls. | Admin login; user creation / role edits and all 13 approval grants remain separate operations. | round2-settings. No role or grant changes performed. |
| ACCESS-003 | Bulk PO deletion / restore and deleted-order recovery | Ashok's active pipeline has no admin selection/deletion/restore controls. | Admin; actual deletion also conflicts with the instruction to retain active test records, so it is not performed in this live run. | final-pipeline-handoff. No deleted test PO. |
| DEP-001 | New ERP setup; QA-045,053,069–079,086,150 | QAAS01 has no ERP variants; the live build offers no Add ERP item entry point. Existing real product definitions are preserved. | Publish and retest BUG-005; then create and approve synthetic brand variants through their normal controls. This is not established as an Admin-only restriction. | qa-erp-search, case02-blocked-live, r2-plm-open. |
| DEP-002 | Vendor / item / price workbook preview scenarios | Vendor master shows retained import history; Item master shows Base/ERP/category/import-history tabs, with no workbook upload entry point on these live pages. | Verify the intended supported import workflow against DEC-007/025 before exposing older import code. A code function alone is not a live UI path or proof of Admin access. | round2-vendors, r2pages-001, r2pages-064. Freight and tracking imports are separately reachable and tested. |
| ENV-001 | Other-role denial, expired sessions, backend faults, backup/restore and volume fixtures | These conditions cannot be established by Ashok's normal live browser controls without changing roles, interrupting service or creating operational load. | Controlled isolated environment and the relevant accounts/infrastructure owner. Report isolated evidence separately from live browser results. | Explicit scenario preconditions in QA_TEST_MATRIX.md. Never mark these passed from unrelated happy-path evidence. |

Approval stages to review in Admin → Users & settings → Approval controls: PO approval/issue; return PO; amendment approval; PI approval; milestone authorization; void payment record; initial-payment completion/automatic authorization; order artwork approval; technical specification approval; technical rejection; brand-specific change approval; brand artwork approval; pre-production sample approval/rejection. Admin remains authoritative. Approve and reject permissions must be checked independently, as ACCESS-001 demonstrates.

No role switch, grant relaxation, cleanup or publication is implied by this register. Each dependent test remains open until the stated prerequisite is supplied and that test is rerun.


## Final coverage accounting

235 PASS / 21 FAIL / 84 BLOCKED; no NOT RUN rows. These 84 cases remain unexecuted/partially verified for stated prerequisites, not passed.

Additional dependencies:

- DEP-003: vendor View lacks a per-record creation audit showing actor and before/after values (QA-035); need a supported audit UI/evidence path.
- DEP-004: QA12 shipment QA-0913-12-20GP is retained PLANNED. Only real40HC benchmarks exist; matching20GP benchmark is required before booking. Manager can import the correct supplied sheet; Admin is not inherently required. No unrelated benchmark selected.
- EVIDENCE-001 — RESOLVED: after QA12 receipt corrections, the original evidence was downloaded through the browser. The fresh225-byte download matches the original synthetic file by SHA-256. No live API/database read was used. See round2-download-integrity.
- ENV-002: QA-003/006 authentication-boundary checks need a separate login session. Ashok's manually authenticated live session was retained for handoff.

| Scenario | Blocker / next prerequisite |
|---|---|
| QA-002 — Invalid password | ENV-001: this scenario explicitly requires In isolated fixture submit one wrong password. It was not induced on the live Ashok-only session. Requires a controlled fixture/other account; local regression evidence is reported separately. |
| QA-003 — Missing email | ENV-002: Ashok remains manually authenticated for handoff. Anonymous empty-email/logout-and-relogin checks need a separate authentication session; no credential guessing or invalidation of the retained session was performed. |
| QA-004 — Invalid session | ENV-001: this scenario explicitly requires In isolated fixture invalidate session then reload protected order. It was not induced on the live Ashok-only session. Requires a controlled fixture/other account; local regression evidence is reported separately. |
| QA-005 — Expired session | ENV-001: this scenario explicitly requires In isolated fixture expire session during open form; submit. It was not induced on the live Ashok-only session. Requires a controlled fixture/other account; local regression evidence is reported separately. |
| QA-006 — Logout | ENV-002: Ashok remains manually authenticated for handoff. Anonymous empty-email/logout-and-relogin checks need a separate authentication session; no credential guessing or invalidation of the retained session was performed. |
| QA-010 — Duplicate sign-in | ENV-001: this scenario explicitly requires Double-click Sign in in isolated fixture. It was not induced on the live Ashok-only session. Requires a controlled fixture/other account; local regression evidence is reported separately. |
| QA-011 — Server unavailable | ENV-001: this scenario explicitly requires In isolated fixture make bootstrap unavailable; reload. It was not induced on the live Ashok-only session. Requires a controlled fixture/other account; local regression evidence is reported separately. |
| QA-013 — Network interruption | ENV-001: this scenario explicitly requires Disconnect isolated browser during navigation; restore and retry. It was not induced on the live Ashok-only session. Requires a controlled fixture/other account; local regression evidence is reported separately. |
| QA-014 — Malformed response | ENV-001: this scenario explicitly requires Return malformed bootstrap JSON in isolated fixture. It was not induced on the live Ashok-only session. Requires a controlled fixture/other account; local regression evidence is reported separately. |
| QA-018 — Archived supplier | ENV-001: this scenario explicitly requires With isolated archived supplier, reopen historic PO then new PO. It was not induced on the live Ashok-only session. Requires a controlled fixture/other account; local regression evidence is reported separately. |
| QA-024 — Missing payment term | DEP-002: the live master pages have retained import history but no reachable item/vendor/price workbook-upload entry point. This specific preview case requires the supported importer to be exposed/confirmed; freight/tracking coverage is separate. |
| QA-032 — Bad vendor import | DEP-002: the live master pages have retained import history but no reachable item/vendor/price workbook-upload entry point. This specific preview case requires the supported importer to be exposed/confirmed; freight/tracking coverage is separate. |
| QA-033 — Identical vendor reimport | DEP-002: the live master pages have retained import history but no reachable item/vendor/price workbook-upload entry point. This specific preview case requires the supported importer to be exposed/confirmed; freight/tracking coverage is separate. |
| QA-035 — Vendor audit | DEP-003: Vendor View exposes identity/defaults and import/update-request history, but no per-record creation audit with actor and before/after values. Ashok UI cannot verify this audit assertion; do not infer it from database code. |
| QA-040 — Missing supplier mapping | DEP-002: the live master pages have retained import history but no reachable item/vendor/price workbook-upload entry point. This specific preview case requires the supported importer to be exposed/confirmed; freight/tracking coverage is separate. |
| QA-044 — Invalid brand | DEP-002: the live master pages have retained import history but no reachable item/vendor/price workbook-upload entry point. This specific preview case requires the supported importer to be exposed/confirmed; freight/tracking coverage is separate. |
| QA-045 — Duplicate ERP code | DEP-001 / BUG-005: synthetic approved QAAS01/QAAS02 have no ERP variants and the live build lacks Add ERP item. Publish/retest the local ERP setup fix, then run this dependent scenario without changing real product masters. |
| QA-047 — Inactive base | ENV-001: this scenario explicitly requires In isolated fixture make QA base inactive; create PO. It was not induced on the live Ashok-only session. Requires a controlled fixture/other account; local regression evidence is reported separately. |
| QA-053 — Fallback TAT | DEP-001 / BUG-005: synthetic approved QAAS01/QAAS02 have no ERP variants and the live build lacks Add ERP item. Publish/retest the local ERP setup fix, then run this dependent scenario without changing real product masters. |
| QA-055 — Duplicate item import | DEP-002: the live master pages have retained import history but no reachable item/vendor/price workbook-upload entry point. This specific preview case requires the supported importer to be exposed/confirmed; freight/tracking coverage is separate. |
| QA-056 — Non-authoritative price rows | DEP-002: the live master pages have retained import history but no reachable item/vendor/price workbook-upload entry point. This specific preview case requires the supported importer to be exposed/confirmed; freight/tracking coverage is separate. |
| QA-067 — Reject revision | ACCESS-001: QAAS01 QA-1.1 pending; Ashok has Approve but no Reject control. Retained for Admin/configured approver. |
| QA-069 — Issued snapshot immutability | DEP-001 / BUG-005: synthetic approved QAAS01/QAAS02 have no ERP variants and the live build lacks Add ERP item. Publish/retest the local ERP setup fix, then run this dependent scenario without changing real product masters. |
| QA-070 — GJ brand difference | DEP-001 / BUG-005: synthetic approved QAAS01/QAAS02 have no ERP variants and the live build lacks Add ERP item. Publish/retest the local ERP setup fix, then run this dependent scenario without changing real product masters. |
| QA-071 — KD brand difference | DEP-001 / BUG-005: synthetic approved QAAS01/QAAS02 have no ERP variants and the live build lacks Add ERP item. Publish/retest the local ERP setup fix, then run this dependent scenario without changing real product masters. |
| QA-072 — TT brand difference | DEP-001 / BUG-005: synthetic approved QAAS01/QAAS02 have no ERP variants and the live build lacks Add ERP item. Publish/retest the local ERP setup fix, then run this dependent scenario without changing real product masters. |
| QA-073 — Colour mapping | DEP-001 / BUG-005: synthetic approved QAAS01/QAAS02 have no ERP variants and the live build lacks Add ERP item. Publish/retest the local ERP setup fix, then run this dependent scenario without changing real product masters. |
| QA-074 — Sticker requirement | DEP-001 / BUG-005: synthetic approved QAAS01/QAAS02 have no ERP variants and the live build lacks Add ERP item. Publish/retest the local ERP setup fix, then run this dependent scenario without changing real product masters. |
| QA-075 — Embossing requirement | DEP-001 / BUG-005: synthetic approved QAAS01/QAAS02 have no ERP variants and the live build lacks Add ERP item. Publish/retest the local ERP setup fix, then run this dependent scenario without changing real product masters. |
| QA-076 — Brand approval | DEP-001 / BUG-005: synthetic approved QAAS01/QAAS02 have no ERP variants and the live build lacks Add ERP item. Publish/retest the local ERP setup fix, then run this dependent scenario without changing real product masters. |
| QA-077 — Multiple artwork files | DEP-001 / BUG-005: synthetic approved QAAS01/QAAS02 have no ERP variants and the live build lacks Add ERP item. Publish/retest the local ERP setup fix, then run this dependent scenario without changing real product masters. |
| QA-078 — Artwork revision | DEP-001 / BUG-005: synthetic approved QAAS01/QAAS02 have no ERP variants and the live build lacks Add ERP item. Publish/retest the local ERP setup fix, then run this dependent scenario without changing real product masters. |
| QA-079 — Independent revision tracks | DEP-001 / BUG-005: synthetic approved QAAS01/QAAS02 have no ERP variants and the live build lacks Add ERP item. Publish/retest the local ERP setup fix, then run this dependent scenario without changing real product masters. |
| QA-086 — Existing approved spec gate | DEP-001 / BUG-005: synthetic approved QAAS01/QAAS02 have no ERP variants and the live build lacks Add ERP item. Publish/retest the local ERP setup fix, then run this dependent scenario without changing real product masters. |
| QA-116 — Natural PO sort | ENV-001: this scenario explicitly requires Sort QA-2 and QA-10 in isolated fixture. It was not induced on the live Ashok-only session. Requires a controlled fixture/other account; local regression evidence is reported separately. |
| QA-150 — Supplier fallback | DEP-001 / BUG-005: synthetic approved QAAS01/QAAS02 have no ERP variants and the live build lacks Add ERP item. Publish/retest the local ERP setup fix, then run this dependent scenario without changing real product masters. |
| QA-247 — Missing route rate | Created retained20GP shipment; booking requires benchmark but only40HC routes are available. No unrelated benchmark selected. DEP-004: supply matching20GP rate before booking. |
| QA-253 — Above99 | ENV-001: this scenario explicitly requires Book isolated freight4019 against3920. It was not induced on the live Ashok-only session. Requires a controlled fixture/other account; local regression evidence is reported separately. |
| QA-267 — Duplicate submission | ENV-001: this scenario explicitly requires Double-click one complaint save in isolated fixture. It was not induced on the live Ashok-only session. Requires a controlled fixture/other account; local regression evidence is reported separately. |
| QA-272 — Valid workbook | DEP-002: the live master pages have retained import history but no reachable item/vendor/price workbook-upload entry point. This specific preview case requires the supported importer to be exposed/confirmed; freight/tracking coverage is separate. |
| QA-273 — Extra columns | DEP-002: the live master pages have retained import history but no reachable item/vendor/price workbook-upload entry point. This specific preview case requires the supported importer to be exposed/confirmed; freight/tracking coverage is separate. |
| QA-274 — Missing required heading | DEP-002: the live master pages have retained import history but no reachable item/vendor/price workbook-upload entry point. This specific preview case requires the supported importer to be exposed/confirmed; freight/tracking coverage is separate. |
| QA-275 — Blank row | DEP-002: the live master pages have retained import history but no reachable item/vendor/price workbook-upload entry point. This specific preview case requires the supported importer to be exposed/confirmed; freight/tracking coverage is separate. |
| QA-276 — Whitespace | DEP-002: the live master pages have retained import history but no reachable item/vendor/price workbook-upload entry point. This specific preview case requires the supported importer to be exposed/confirmed; freight/tracking coverage is separate. |
| QA-277 — Duplicate item | DEP-002: the live master pages have retained import history but no reachable item/vendor/price workbook-upload entry point. This specific preview case requires the supported importer to be exposed/confirmed; freight/tracking coverage is separate. |
| QA-278 — Duplicate vendor | DEP-002: the live master pages have retained import history but no reachable item/vendor/price workbook-upload entry point. This specific preview case requires the supported importer to be exposed/confirmed; freight/tracking coverage is separate. |
| QA-279 — Mixed-case code | DEP-002: the live master pages have retained import history but no reachable item/vendor/price workbook-upload entry point. This specific preview case requires the supported importer to be exposed/confirmed; freight/tracking coverage is separate. |
| QA-280 — Trailing spaces | DEP-002: the live master pages have retained import history but no reachable item/vendor/price workbook-upload entry point. This specific preview case requires the supported importer to be exposed/confirmed; freight/tracking coverage is separate. |
| QA-281 — Invalid currency | DEP-002: the live master pages have retained import history but no reachable item/vendor/price workbook-upload entry point. This specific preview case requires the supported importer to be exposed/confirmed; freight/tracking coverage is separate. |
| QA-282 — Invalid number | DEP-002: the live master pages have retained import history but no reachable item/vendor/price workbook-upload entry point. This specific preview case requires the supported importer to be exposed/confirmed; freight/tracking coverage is separate. |
| QA-283 — Empty price | DEP-002: the live master pages have retained import history but no reachable item/vendor/price workbook-upload entry point. This specific preview case requires the supported importer to be exposed/confirmed; freight/tracking coverage is separate. |
| QA-284 — Zero price | DEP-002: the live master pages have retained import history but no reachable item/vendor/price workbook-upload entry point. This specific preview case requires the supported importer to be exposed/confirmed; freight/tracking coverage is separate. |
| QA-285 — Negative price | DEP-002: the live master pages have retained import history but no reachable item/vendor/price workbook-upload entry point. This specific preview case requires the supported importer to be exposed/confirmed; freight/tracking coverage is separate. |
| QA-286 — Invalid TAT | DEP-002: the live master pages have retained import history but no reachable item/vendor/price workbook-upload entry point. This specific preview case requires the supported importer to be exposed/confirmed; freight/tracking coverage is separate. |
| QA-287 — Unknown supplier | DEP-002: the live master pages have retained import history but no reachable item/vendor/price workbook-upload entry point. This specific preview case requires the supported importer to be exposed/confirmed; freight/tracking coverage is separate. |
| QA-288 — Inactive supplier | DEP-002: the live master pages have retained import history but no reachable item/vendor/price workbook-upload entry point. This specific preview case requires the supported importer to be exposed/confirmed; freight/tracking coverage is separate. |
| QA-289 — Duplicate supplier code | DEP-002: the live master pages have retained import history but no reachable item/vendor/price workbook-upload entry point. This specific preview case requires the supported importer to be exposed/confirmed; freight/tracking coverage is separate. |
| QA-290 — Duplicate price | DEP-002: the live master pages have retained import history but no reachable item/vendor/price workbook-upload entry point. This specific preview case requires the supported importer to be exposed/confirmed; freight/tracking coverage is separate. |
| QA-291 — Identical reimport | DEP-002: the live master pages have retained import history but no reachable item/vendor/price workbook-upload entry point. This specific preview case requires the supported importer to be exposed/confirmed; freight/tracking coverage is separate. |
| QA-292 — Changed price reimport | DEP-002: the live master pages have retained import history but no reachable item/vendor/price workbook-upload entry point. This specific preview case requires the supported importer to be exposed/confirmed; freight/tracking coverage is separate. |
| QA-293 — Viewer boundaries | ENV-001: this scenario explicitly requires In isolated fixture sign in VIEWER and inspect mutation controls. It was not induced on the live Ashok-only session. Requires a controlled fixture/other account; local regression evidence is reported separately. |
| QA-294 — Executive boundaries | ENV-001: this scenario explicitly requires In isolated fixture use EXECUTIVE on another owner order. It was not induced on the live Ashok-only session. Requires a controlled fixture/other account; local regression evidence is reported separately. |
| QA-296 — Product Manager boundaries | ENV-001: this scenario explicitly requires In isolated fixture use PRODUCT_MANAGER for purchase approval. It was not induced on the live Ashok-only session. Requires a controlled fixture/other account; local regression evidence is reported separately. |
| QA-297 — Admin boundaries | ENV-001: this scenario explicitly requires In isolated fixture inspect ADMIN user controls. It was not induced on the live Ashok-only session. Requires a controlled fixture/other account; local regression evidence is reported separately. |
| QA-298 — Unapproved role approval | ENV-001: this scenario explicitly requires In isolated fixture remove role from stage and attempt approve. It was not induced on the live Ashok-only session. Requires a controlled fixture/other account; local regression evidence is reported separately. |
| QA-299 — Master edit denial | ENV-001: this scenario explicitly requires In isolated fixture VIEWER attempts vendor save. It was not induced on the live Ashok-only session. Requires a controlled fixture/other account; local regression evidence is reported separately. |
| QA-301 — Foreign order edit | ENV-001: this scenario explicitly requires In isolated fixture EXECUTIVE targets another owner PO. It was not induced on the live Ashok-only session. Requires a controlled fixture/other account; local regression evidence is reported separately. |
| QA-302 — Price scope denial | ENV-001: this scenario explicitly requires In isolated fixture target unassigned division base. It was not induced on the live Ashok-only session. Requires a controlled fixture/other account; local regression evidence is reported separately. |
| QA-303 — Shipping scope denial | ENV-001: this scenario explicitly requires In isolated fixture attempt out-of-scope shipment update. It was not induced on the live Ashok-only session. Requires a controlled fixture/other account; local regression evidence is reported separately. |
| QA-314 — Double Save | ENV-001: this scenario explicitly requires Double-click draft Save in isolated fixture. It was not induced on the live Ashok-only session. Requires a controlled fixture/other account; local regression evidence is reported separately. |
| QA-315 — Double Submit | ENV-001: this scenario explicitly requires Double-click Submit in isolated fixture. It was not induced on the live Ashok-only session. Requires a controlled fixture/other account; local regression evidence is reported separately. |
| QA-316 — Double Payment | ENV-001: this scenario explicitly requires Double-click Save payment in isolated fixture. It was not induced on the live Ashok-only session. Requires a controlled fixture/other account; local regression evidence is reported separately. |
| QA-322 — Two-user edit | ENV-001: this scenario explicitly requires In isolated fixture submit concurrent edits with same revision. It was not induced on the live Ashok-only session. Requires a controlled fixture/other account; local regression evidence is reported separately. |
| QA-323 — Stale own tab | ENV-001: this scenario explicitly requires In isolated fixture two Ashok tabs save conflicting notes. It was not induced on the live Ashok-only session. Requires a controlled fixture/other account; local regression evidence is reported separately. |
| QA-324 — Upload interruption | ENV-001: this scenario explicitly requires Interrupt isolated second file upload; retry. It was not induced on the live Ashok-only session. Requires a controlled fixture/other account; local regression evidence is reported separately. |
| QA-325 — Issue interruption | ENV-001: this scenario explicitly requires Drop isolated issue response after commit; reopen. It was not induced on the live Ashok-only session. Requires a controlled fixture/other account; local regression evidence is reported separately. |
| QA-326 — Server timeout | ENV-001: this scenario explicitly requires Delay isolated response beyond timeout then retry. It was not induced on the live Ashok-only session. Requires a controlled fixture/other account; local regression evidence is reported separately. |
| QA-327 — Malformed response | ENV-001: this scenario explicitly requires Return malformed isolated command response. It was not induced on the live Ashok-only session. Requires a controlled fixture/other account; local regression evidence is reported separately. |
| QA-328 — Database failure | ENV-001: this scenario explicitly requires Inject isolated transaction failure. It was not induced on the live Ashok-only session. Requires a controlled fixture/other account; local regression evidence is reported separately. |
| QA-329 — File storage failure | ENV-001: this scenario explicitly requires Inject isolated file write failure. It was not induced on the live Ashok-only session. Requires a controlled fixture/other account; local regression evidence is reported separately. |
| QA-332 — Huge remarks | ENV-001: this scenario explicitly requires In isolated fixture submit very long remarks. It was not induced on the live Ashok-only session. Requires a controlled fixture/other account; local regression evidence is reported separately. |
| QA-336 — 1000 orders | ENV-001: this scenario explicitly requires Load isolated1000-order fixture and sort/filter. It was not induced on the live Ashok-only session. Requires a controlled fixture/other account; local regression evidence is reported separately. |
| QA-337 — 5000 shipments | ENV-001: this scenario explicitly requires Load isolated large shipment fixture. It was not induced on the live Ashok-only session. Requires a controlled fixture/other account; local regression evidence is reported separately. |
| QA-338 — Large audit | ENV-001: this scenario explicitly requires Open isolated10000-event history. It was not induced on the live Ashok-only session. Requires a controlled fixture/other account; local regression evidence is reported separately. |
