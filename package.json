{
  "name": "farming-hub-purchase-management-pilot",
  "version": "0.6.1-alpha.16",
  "private": true,
  "type": "module",
  "description": "Base-first purchasing with controlled missing-PLM warning bypass, ERP item codes, price lists, complaints and freight trend controls.",
  "engines": {
    "node": ">=22.16.0"
  },
  "scripts": {
    "start": "node server/index.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {}
}
