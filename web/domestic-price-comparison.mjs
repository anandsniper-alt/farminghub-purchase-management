import {compareDomesticSuppliers,compareDomesticQuotes,domesticQuoteHistory} from '../shared/domestic-prices.mjs';
import {softwareReference} from '../shared/references.mjs';
import {compareDomesticAssemblySuppliers,domesticIsAssembly,DOMESTIC_SCOPE} from '../shared/domestic.mjs';

export function createDomesticPriceComparison(host){
 const {context,esc,field,note,showDialog,renderModal,money,picture,identity,hydrate,badge}=host;
 let view=null;
 const state=()=>context().state;
 function vendors(){const map=new Map();for(const q of state().domesticPriceLists||[])map.set(q.vendorId,{id:q.vendorId,code:q.vendorCode,name:q.vendorName});return [...map.values()].sort((a,b)=>a.code.localeCompare(b.code));}
 function assemblies(){return (state().domesticBoms||[]).filter(b=>b.scope===DOMESTIC_SCOPE&&domesticIsAssembly(b));}
 function resetQuotes(){const history=domesticQuoteHistory(state(),view.vendorId);view.oldId=history.at(-2)?.id||'';view.newId=history.at(-1)?.id||'';}
 function options(){return [{value:'',label:'Select quotation'},...domesticQuoteHistory(state(),view.vendorId).slice().reverse().map(q=>({value:q.id,label:q.quoteDate+' · '+q.quoteReference+' · '+softwareReference(state(),'domesticPriceLists',q.id)}))];}
 function priceCell(line){return line?money(line.rateMinor)+'<div class="sub">per '+esc(line.uom)+(line.rateMinor===0?' · Zero rate':'')+'</div>':'<span class="muted">Not quoted</span>';}
 function assemblyTable(filter){
  const selected=vendors().filter(v=>view.vendorIds.includes(v.id));
  if(!selected.length)return note('Select at least one supplier.');
  let result;try{result=compareDomesticAssemblySuppliers(state(),view.assemblyId,selected.map(v=>v.id),view.baselineVendorId);}catch(e){return note(e.message,'warn');}
  const baseline=selected.find(v=>v.id===result.baselineVendorId),rows=result.rows.filter(r=>filter(r.line)),qty=l=>l.quantityMilli==null?'Pending':Number(l.quantityMilli/1000).toLocaleString('en-IN',{maximumFractionDigits:3});
  const totals='<div class="table-wrap"><table class="domestic-price-comparison" aria-label="Total assembly cost by supplier"><thead><tr><th>Supplier</th><th>Total for 1 assembly</th><th>Parts priced</th><th>Difference vs '+esc(baseline?.code||'reference')+'</th><th>Status</th></tr></thead><tbody>'+result.cells.map((c,index)=>'<tr><td><b>'+esc(selected[index].code)+'</b><div class="sub">'+esc(selected[index].name)+'</div></td><td>'+(c.complete?'<b>'+money(c.totalMinor)+'</b>':badge('Incomplete','amber')+'<div class="sub">Priced parts subtotal: '+(c.knownMinor==null?'Unavailable':money(c.knownMinor))+'</div>')+'</td><td>'+c.pricedParts+' / '+result.rows.length+'</td><td>'+(c.deltaMinor==null?'—<div class="sub">Complete both totals to compare</div>':c.vendorId===result.baselineVendorId?'Reference supplier':(c.deltaMinor>0?'+':'')+money(c.deltaMinor)+'<div class="sub">'+esc(c.percentText||'Percentage unavailable: reference total is zero')+'</div>')+'</td><td>'+(c.issue?esc(c.issue):!c.complete?badge(result.issues.length||result.rows.some(r=>r.cells[index].issue==='Enter a valid BOM quantity.'||r.cells[index].issue==='Use purchased parts in this assembly BOM.')?'BOM incomplete':'Incomplete quote','amber'):c.lowest?badge('Lowest complete total','green'):badge('Complete','gray'))+'</td></tr>').join('')+'</tbody></table></div>';
  const details=!rows.length?note(result.rows.length?'No assembly parts match this search. The totals still include all parts.':'Add parts and quantities in Edit BOM to compare this assembly.'):'<div class="table-wrap"><table class="domestic-price-comparison" aria-label="Assembly parts supplier breakdown"><thead><tr><th>Picture</th><th>Assembly part</th><th>Qty for 1 assembly</th>'+selected.map(v=>'<th>'+esc(v.code)+'<div class="sub">Rate / part cost</div></th>').join('')+'</tr></thead><tbody>'+rows.map(r=>'<tr><td>'+picture(r.line)+'</td><td><b>'+esc(r.line.code)+'</b><div>'+esc(r.line.description)+'</div></td><td>'+esc(qty(r.line))+' '+esc(r.line.uom)+'</td>'+r.cells.map(c=>'<td>'+(c.quote?priceCell(c.quote)+'<div class="sub">'+esc(c.quote.quoteReference)+' · '+esc(c.quote.quoteDate)+'</div>':'')+(c.issue?badge(c.issue,'amber'):'<b>Part cost: '+money(c.amountMinor)+'</b>')+'</td>').join('')+'</tr>').join('')+'</tbody></table></div>';
  return '<p><b>'+esc(result.bom.code+' · '+result.bom.name)+'</b><span class="sub"> · Saved revision '+result.bom.revision+'</span></p>'+result.issues.map(m=>note(m,'warn')).join('')+totals+'<p class="small muted">For 1 assembly · INR before GST, freight and other charges. Latest saved quote per part; dates may differ. Incomplete quotes are excluded from the lowest total. Search filters the breakdown only.</p><h3>Assembly parts</h3>'+details;
 }
 function table(){
  const filter=item=>(item.code+' '+item.description).toLowerCase().includes(view.query.trim().toLowerCase());
  if(view.mode==='ASSEMBLY')return assemblyTable(filter);
  if(view.mode==='SUPPLIERS'){
   const selected=vendors().filter(v=>view.vendorIds.includes(v.id)),rows=compareDomesticSuppliers(state(),selected.map(v=>v.id)).filter(r=>filter(r.item));
   if(!selected.length)return note('Select at least one supplier.');if(!rows.length)return note('No quoted items match this selection.');
   return '<p class="small muted">Latest quote by date for each item and supplier. Same-date ties use the last saved quote. Missing prices are not zero. Lowest marks comparable unit rates only.</p><div class="table-wrap"><table class="domestic-price-comparison" aria-label="Supplier prices side by side"><thead><tr><th>Picture</th><th>Item / BOM code</th><th>UOM</th>'+selected.map(v=>'<th>'+esc(v.code)+'<div class="sub">'+esc(v.name)+'</div></th>').join('')+'</tr></thead><tbody>'+rows.map(r=>'<tr><td>'+picture(r.item)+'</td><td>'+identity(r.item)+'</td><td>'+esc(r.item.uom)+'</td>'+r.cells.map(c=>'<td>'+priceCell(c.quote)+(c.quote?'<div class="sub">'+esc(c.quote.quoteReference)+' · '+esc(c.quote.quoteDate)+'</div>':'')+(c.unitMismatch?badge('Different UOM','amber'):c.lowest?badge('Lowest','green'):'')+'</td>').join('')+'</tr>').join('')+'</tbody></table></div>';
  }
  if(!view.oldId||!view.newId)return note('Choose two quotations from this supplier. Save another quotation if only one is available.');
  let compared;try{compared=compareDomesticQuotes(state(),view.vendorId,view.oldId,view.newId);}catch(e){return note(e.message,'warn');}
  const rows=compared.rows.filter(r=>filter(r.item));if(!rows.length)return note('No quoted items match this search.');
  const labels={NEW_ITEM:'Newly quoted',NOT_QUOTED:'Not in new quote',UOM_CHANGED:'Different UOM',INCREASED:'Increased',DECREASED:'Decreased',UNCHANGED:'Unchanged'};
  return '<p class="small muted">Only the two selected quotations are compared. A missing item is not treated as zero. Percentage change is unavailable when the old rate is zero or the UOM differs.</p><div class="table-wrap"><table class="domestic-price-comparison" aria-label="Old and new supplier prices"><thead><tr><th>Picture</th><th>Item / BOM code</th><th>Old price<div class="sub">'+esc(compared.oldQuote.quoteReference)+' · '+esc(compared.oldQuote.quoteDate)+'</div></th><th>New price<div class="sub">'+esc(compared.newQuote.quoteReference)+' · '+esc(compared.newQuote.quoteDate)+'</div></th><th>Difference</th><th>Change</th></tr></thead><tbody>'+rows.map(r=>'<tr><td>'+picture(r.item)+'</td><td>'+identity(r.item)+'</td><td>'+priceCell(r.oldLine)+'</td><td>'+priceCell(r.newLine)+'</td><td>'+(r.deltaMinor==null?'—':(r.deltaMinor>0?'+':'')+money(r.deltaMinor))+'<div class="sub">'+(r.percentText||'Percentage not applicable')+'</div></td><td>'+badge(labels[r.status],r.status==='DECREASED'?'green':r.status==='INCREASED'?'red':r.status==='UNCHANGED'?'gray':'amber')+'</td></tr>').join('')+'</tbody></table></div>';
 }
 function refresh(){const target=document.getElementById('domestic-compare-results');if(target){target.innerHTML=table();hydrate();}}
 function modal(){
  queueMicrotask(refresh);
  const list=vendors(),assemblyControls=view.mode==='ASSEMBLY'?'<div class="form-grid">'+field('priceCompareAssembly','Assembly BOM',view.assemblyId,'select',{options:[{value:'',label:'Select assembly'},...assemblies().map(b=>({value:b.id,label:b.code+' · '+b.name+' · Rev '+b.revision}))]})+field('priceCompareBaseline','Compare against',view.baselineVendorId,'select',{options:list.filter(v=>view.vendorIds.includes(v.id)).map(v=>({value:v.id,label:v.code+' · '+v.name}))})+'</div>':'';
  return {title:'Compare supplier prices',sub:view.mode==='ASSEMBLY'?'Total for 1 assembly · INR before GST, freight and other charges':'INR per item UOM · before GST, freight and other charges',wide:true,submit:null,body:!list.length?note('Save a supplier price list using Enter prices or Upload price list, then return here to compare.'):field('priceCompareMode','Comparison',view.mode,'select',{options:[{value:'SUPPLIERS',label:'Across suppliers'},{value:'HISTORY',label:'Same supplier · old vs new'},{value:'ASSEMBLY',label:'Assembly totals'}]})+assemblyControls+(view.mode!=='HISTORY'?'<fieldset class="domestic-compare-suppliers"><legend>Suppliers to compare</legend>'+list.map(v=>'<label><input type="checkbox" data-compare-supplier="'+esc(v.id)+'" '+(view.vendorIds.includes(v.id)?'checked':'')+'> '+esc(v.code+' · '+v.name)+'</label>').join('')+'</fieldset>':'<div class="form-grid">'+field('priceCompareVendor','Supplier',view.vendorId,'select',{options:list.map(v=>({value:v.id,label:v.code+' · '+v.name}))})+field('priceCompareOld','Old quotation',view.oldId,'select',{options:options()})+field('priceCompareNew','New quotation',view.newId,'select',{options:options()})+'</div>')+field('priceCompareSearch','Find item',view.query,'search',{attrs:'placeholder="Item name or code"'})+'<div id="domestic-compare-results" aria-live="polite"></div>'};
 }
 function action(a,d={}){if(!['domestic-price-compare','domestic-price-compare-assembly'].includes(a))return false;const list=vendors();view={mode:a==='domestic-price-compare-assembly'?'ASSEMBLY':'SUPPLIERS',assemblyId:d.id||assemblies().find(b=>b.lines.length)?.id||assemblies()[0]?.id||'',baselineVendorId:list[0]?.id||'',vendorIds:list.map(v=>v.id),vendorId:list[0]?.id||'',oldId:'',newId:'',query:''};resetQuotes();showDialog('domestic-price-compare',{});return true;}
 function change(el){if(context().ui.modal!=='domestic-price-compare')return false;
  if(el.name==='priceCompareMode'||el.name==='priceCompareVendor'){if(el.name==='priceCompareMode')view.mode=el.value;else{view.vendorId=el.value;resetQuotes();}renderModal();queueMicrotask(()=>document.querySelector('[name="'+el.name+'"]')?.focus());return true;}
  if(el.hasAttribute('data-compare-supplier')){view.vendorIds=[...document.querySelectorAll('[data-compare-supplier]:checked')].map(e=>e.dataset.compareSupplier);if(view.mode==='ASSEMBLY'){if(!view.vendorIds.includes(view.baselineVendorId))view.baselineVendorId=view.vendorIds[0]||'';renderModal();queueMicrotask(()=>[...document.querySelectorAll('[data-compare-supplier]')].find(e=>e.dataset.compareSupplier===el.dataset.compareSupplier)?.focus());}else refresh();return true;}
  if(el.name==='priceCompareAssembly'||el.name==='priceCompareBaseline'){view[el.name==='priceCompareAssembly'?'assemblyId':'baselineVendorId']=el.value;refresh();return true;}
  if(el.name==='priceCompareOld'||el.name==='priceCompareNew'){view[el.name==='priceCompareOld'?'oldId':'newId']=el.value;refresh();return true;}
  if(el.name==='priceCompareSearch'){view.query=el.value;refresh();return true;}return false;
 }
 return {modal,action,change};
}
